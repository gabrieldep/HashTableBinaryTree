#include <iostream>
#include <string>
#include <regex>
#include <fstream>
#include "Utils.h"

using namespace std;

int main()
{
	string line;
	ifstream myfile;
	int melhoriaAlg = 4;
	myfile.open("entrada_1.txt");
	while (getline(myfile, line))
	{
	}
	myfile.close();
}